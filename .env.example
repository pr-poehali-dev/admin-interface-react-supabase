# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Database Schema (для справки)
# Таблицы в Supabase:
# - users (id, email, username, avatar_url, role, created_at, updated_at)
# - videos (id, title, description, url, thumbnail_url, duration, views, likes, dislikes, status, created_at, updated_at, user_id, category_id)
# - categories (id, name, slug, description, color, created_at, updated_at)
# - comments (id, content, video_id, user_id, parent_id, status, created_at, updated_at)
# - analytics (id, video_id, views, date, user_id)
# - moderation_logs (id, video_id, moderator_id, action, reason, created_at)

# Row Level Security (RLS) должен быть настроен для безопасности
# Роли: admin, moderator, user